import subprocess ## used in the trigger script to initiate the actual terminal.
import os ## used to load modules
import sys ## used to kill in case of failure
from settings import *
if DEBUG:
	from debugtools import *

