Using Sublime Text 2, with githubs git for windows application (Winforms windows 8 based?)


Seems to work for now. Cygwin = pain. (ssh not working correctly)
- expected exec `ssh-agent` to be run before on that term session
- ssh-agent crashed term session when run (?)
